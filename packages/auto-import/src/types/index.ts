export interface AntdResolverOptions {
  /**
   * The prefix of the antd component name.
   */
  prefix?: string
  /**
   * The package name of the antd component.
   * @default antd
   */
  packageName?: string
}
